refactor: prune unused files, update load test harness, and fix handlers

Removed stale cache and monitoring modules, updated package metadata, and adjusted TypeScript settings.

Details:
- Deleted legacy caching/monitoring files: audio-cache.ts, transcript-cache.ts, cost-monitor.ts, and package-lock.json (now using pnpm).
- Updated `package.json` scripts and dependency list to streamline dev flow and add compatibility with `pnpm`.
- Modified load test harness under `load/`: refined `run.ts`, `scenario.ts`, and `worker.ts` to improve concurrency controls and chaos testing flags.
- Fixed handler logic and typings in `handlers/FormFillHandler.ts` and `handlers/NotesHandler.ts` to match refactored interfaces.
- Minor updates across: `parse-gpt.ts`, `tsconfig.json`, `types.ts`, `ws-token.ts`, and tests under `test/` to align with the refactor.

Build: TypeScript compilation passes (`pnpm build`).

Testing: manual test scripts available: `pnpm run test-client`, `pnpm run test-client:single`, `pnpm run test-client:live`, and load test scripts prefixed with `load-test`.
