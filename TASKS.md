# Tasks

Use this file as the working ticket list for future AI-agent turns. Work on only the requested ticket ID. Keep backend-local IDs canonical here; keep frontend/product tickets in the cross-repo reference section instead of mixing them into the backend backlog.

## Completed

| ID | Status | Notes |
| --- | --- | --- |
| T-001 Fix test-client ESM/ts-node import issue | Completed | Test clients use the current mode/token start contract via the `tsx` runner; no root `.js` shims. |
| T-002 Improve load-test failure logging and default WS_URL | Completed | Load tests default to `ws://localhost:5551`, allow `WS_URL`, and print per-VU failure reasons. |
| T-003 Verify WS_TOKEN_SECRET fails closed | Completed | Auth test confirms missing/invalid/expired token and mode mismatch fail closed; valid token reaches `started`. |
| T-004 Smoke test WebSocket recording flow | Completed | Start/stop flow verified with valid mode/token sessions; recording reaches `started` reliably. |
| T-005 Long-session memory / rolling synthesis (Phase 1) | Completed | Notes final pass uses a larger notes-only transcript window with capped output-token budget and token diagnostics. Rolling checkpoint digests (Option B, for beyond-cap scaling) tracked as T-017. |
| T-006 Harden form extraction for locked/excluded fields | Completed | Extraction prompts no longer force missing/locked/excluded info into nearby allowed fields. |
| T-007 Add HTTP notes transform endpoints | Completed | Added server-to-server HTTP endpoints for Notes summarise/reorganise on the existing service port, with Bearer secret auth and no WebSocket contract changes. |
| T-008 Improve notes prompts for long training conversations | Completed | Notes prompts reduce duplication, repair fragmented headings, and preserve technical terms for long sessions. |
| T-009 Accept notes continuation context in start payload | Completed | Notes start accepts optional continuation markdown, capped safely, without changing other contracts. |
| T-010 Increase Notes final transcript limit and add safe diagnostics | Completed | Notes finalisation uses a larger notes-only transcript window with content-safe diagnostics; forms unchanged. |
| T-011 Strengthen preservation of canonical current notes | Completed | Hardened finalisation so useful `currentNotesMarkdown` content is preserved unless contradicted, irrelevant, duplicated, artefactual, or unsafe. |
| T-011a Calibrate current-notes preservation eval checks | Completed | P1, backend eval quality. Calibrated Notes-final current-notes preservation fixtures after raw output inspection showed semantic passes with different wording. No production prompt/model/runtime changes. |
| T-012 Reduce notes incremental GPT backlog and stop latency | Completed | Adaptive scheduler batches revised transcript with one in-flight update and a single stop flush, plus notes-only phase-based chunk cadence. |
| T-013 Enforce notes session hard cap | Completed | Server-side session-cap timer drives the same idempotent stop/finalise path as manual Stop. |
| T-014 Notes VAD optimisation | Completed | Skip no-speech batches before Whisper via Silero VAD (ffmpeg-static decode) behind `transcribeAudioBatch`; `VAD_MODE` off/dry-run/gate, fails open, notes gated, forms ungated. |
| T-015 Forms recording observability | Completed | Added content-safe forms-mode timing/observability logging (counts and timings only). |
| T-016 Harden revision fallback and backend session stability | Completed | Forms preserves short values; Forms/Notes suppress stale async sends; Forms stop is idempotent; late audio is ignored after stop; per-session queue caps send `transcription-overloaded`; revision fails open to raw Whisper text; runtime logs use safe metadata only; Notes start payload is sanitised; Whisper/GPT calls have request timeouts. |
| T-019 Summarise notes backend support | Completed | Added backend summarise transform support using current visible `notesMarkdown` only; returns `summaryMarkdown` for frontend preview/apply flows. |
| T-020 Reorganise notes backend support | Completed | Added backend reorganise transform support using current visible `notesMarkdown` and optional target sections only; returns `reorganisedMarkdown` and guards against accidental summarisation. |
| T-062 Align Notes backend cap to 60 minutes | Completed | Backend Notes sessions now use a 60-minute reliability/cost-safety hard cap and finalise through the existing idempotent Stop path. Frontend warning timings remain frontend-owned. |
| T-063 Reduce Notes incremental rewrite cost | Completed | Live notes now use append-only patch JSON applied by the backend to canonical markdown, avoiding repeated full-document live rewrites and bounding live patch output by transcript size rather than current notes length. |
| T-064 Split Notes live-update and final-formatting prompts | Completed | Live prompt now requests small append-only patch instructions; final prompt remains the full-document polish/dedupe/restructure pass and may return shorter final notes when meaning is preserved. |
| T-065 Preserve Notes continuation canonical markdown | Completed | Notes continuation now stores the supplied `currentNotesMarkdown` as full canonical markdown instead of truncating it to 20k chars; final notes may still compact, dedupe, and summarise during `notes_final`. |
| T-066 Clean unused backend constants/code | Completed | Removed unused backend transcription constants and dead cache code after confirming they had no active usage. |
| T-067 Tune Notes batching, revision gates, and live update cadence | Completed | Reduced transcription-overloaded risk by increasing Notes audio batch size over session age, slowing live Notes updates, reducing unnecessary revision GPT calls, raising the live patch output floor, and splitting final-quality reasoning from frequent live/revision calls. |
| T-068 Harden Notes overload recovery support | Completed | Notes overload now pauses intake, sends one existing-shape `transcription-overloaded` error, keeps the socket open, and allows already accepted queued/in-flight work to continue while the socket remains open. |
| T-069 Preserve Notes cap timer across quick reconnects | Completed | Backend cap registry reuses the original 60-minute Notes cap deadline for signed quick reconnects from the same authenticated user and closes the cap window on normal finalisation. |
| T-071 Improve Notes markdown formatting prompts | Completed | Live Notes can create safe provisional `##` sections; final/live prompts improve hierarchy, examples, headings, and live readability without contract changes. |
| T-072 Review and tune Notes/Formify GPT prompts | Completed | Updated active GPT prompts for Forms short values, canonical current notes, adaptive transforms, conservative revision, and no table/sponsor/promo wording. |
| T-075 Split GPT helpers and parsers out of parse-gpt.ts | Completed | Split GPT config, provider, JSON helpers, revision, Forms, Notes live/final, and Notes transform logic into `gpt/` modules while keeping `parse-gpt.ts` as the compatibility facade. |
| T-076 Consolidate safe diagnostics and error helpers | Completed | Added shared content-safe diagnostics helpers for error metadata, JSON keys, token usage, safe values, numeric metadata, and short hashes; refactored low-risk call sites without changing runtime behaviour. |
| T-077 Remove stale backend comments and legacy compatibility residue | Completed | Removed unused historical cache/cost files, stale Groq/parser references, unused legacy dependencies, and misleading comments without changing runtime behaviour or contracts. |
| T-081 Tune Responses output budgets for Notes final and transforms | Completed | Increased Notes final, Summarise, and Reorganise Responses output-budget headroom so medium reasoning is less likely to consume the whole visible-output budget and trigger `max_output_tokens` incompletes. Contracts, prompts, fallback behaviour, and safe diagnostics are unchanged. |
| T-082 Explicitly disable OpenAI response storage | Completed | Set `store: false` on Responses calls through the central provider helper, set type-safe Chat Completions storage defaults where supported, added request-shape tests, and documented the OpenAI data-retention posture. |
| T-083 Evaluate Responses migration for live updates | Completed | Added and ran opt-in live Chat-vs-Responses strict-schema evals for Forms live extraction and Notes live patching. This kept live routing unchanged at the evaluation stage; T-115 later switched Notes live to Responses while Forms live stayed Chat. |
| T-083a Expand live eval fixtures | Completed | Added more Forms live and Notes live fixtures covering sparse unknowns, corrections, explicit N/A, noisy fragments, long current notes, heading reuse, fallback sections, repetition, and side-topic handling. |
| T-083b Redesign Forms live Responses candidate schema | Completed | Replaced the full known-field object candidate with a sparse updates strict schema using known field-key enum values for eval-only Forms live Responses comparisons. Production Chat live routing remains unchanged. |
| T-083c Rerun live-only eval matrix across expanded fixtures | Completed | Ran the expanded live-only Chat-vs-Responses eval matrix across 26 provider cases and updated the T-083 report. Production Chat live routing remains unchanged. |
| T-083d Calibrate live eval failures | Completed | Inspected Forms explicit non-applicability, Notes fallback-section, and Notes unsafe/repeated live eval failures; calibrated fixture/checker expectations only where justified; production live routing remains unchanged. |
| T-083e Add harder Notes live eval fixtures | Completed | Added longer/noisier Notes live fixtures for rolling context, topic shift, repeated correction, and tangent/action handling. Production live routing remains unchanged. |
| T-083f Repeat Notes live Chat-vs-Responses evals | Completed | Ran three opt-in Notes-only Chat-vs-Responses eval repeats and updated the T-083 report with aggregate quality, latency, token, and safety-filter results. Production live routing remains unchanged. |
| T-084 Add GPT quality eval fixtures | Completed | Added representative offline fixtures and deterministic eval helper tests for Forms final extraction, Notes finalisation, Summarise compression, Reorganise preservation, and Notes live patch behaviour. No production model/prompt/routing changes. |
| T-085 Evaluate reasoning/model settings | Completed | Added a test-only reasoning/model evaluation report and experiment plan using the T-084 fixture foundation. No production model, prompt, reasoning, schema, routing, or provider behaviour changed. |
| T-085a Add opt-in OpenAI eval runner | Completed | Added an explicitly opt-in OpenAI-backed eval runner using T-084 fixtures and T-085 experiment metadata. Normal tests remain offline and production model/prompt/routing defaults are unchanged. |
| T-085b Prepare reasoning/model eval execution report | Completed | Prepared the opt-in eval execution/reporting path for current-vs-candidate reasoning/model comparisons. No OpenAI calls were run and no production defaults changed. |
| T-085c Calibrate GPT eval checks against raw outputs | Completed | Inspected opt-in eval outputs locally, adjusted overly phrase-strict deterministic concept checks with explicit fixture-side alternatives, and updated the T-085b report without committing raw model outputs or changing production defaults. |
| T-085d Expand Forms and Summarise eval fixtures | Completed | Added more Forms final fixtures for unknown/empty values, value normalisation, corrections, and explicit N/A, plus more Summarise fixtures for compression. No production defaults changed. |
| T-085e Finalise model/reasoning decisions before live migration | Completed | Ran the expanded opt-in medium-vs-low eval matrix across final/transform fixtures, documented conservative decisions for Forms final, Notes final, Summarise, and Reorganise, and kept production defaults unchanged before T-083. |
| T-090 Evaluate strict structured outputs for live JSON calls | Completed | Covered by T-083. Added strict Responses schema candidates and ran live-only opt-in evals for Forms live extraction and Notes live patching without changing production Chat Completions live paths. |
| T-091 Harden Responses structured output schemas | Completed | Tightened strict Responses parsing/schema checks for final Notes, final Forms, transforms, and live patch fallback paths, including extra-key tests and safe failure handling. |
| T-092 Document OpenAI data-retention posture | Completed | Documented `store: false`, app-owned canonical state, no provider-owned Conversations by default, safe metadata logging only, and no compliance claims from privacy hygiene alone. |
| T-093 Review OpenAI logging and safe diagnostics coverage | Completed | Audited provider/runtime logging for raw content exposure and tightened ambiguous diagnostics to safe metadata only. |
| T-096 Strengthen Summarise compression identity | Completed | Tuned Summarise so long notes become visibly shorter than Reorganise by merging related sections, reducing low-value subheadings, and preserving key process rules, warnings, decisions, actions, examples, and open questions. Hallucination avoidance and uncertainty preservation remain top priorities. |
| T-110 Tighten Summarise compression prompt | Completed | P1, backend quality. Updated Summarise prompt guidance so it behaves like a true condensed summary rather than a light Reorganise. Model/reasoning unchanged. |
| T-110a Verify tightened Summarise prompt | Completed | Ran the Summarise-only opt-in eval after T-110. Compression improved only slightly and RCA concept preservation regressed, so Summarise is not considered fixed yet. No model/reasoning/route changes. |
| T-110b Calibrate Summarise concept preservation | Completed | P1, backend quality. Inspected T-110a Summarise failures and calibrated phrase-strict concept checks where the existing outputs preserved meaning through equivalent wording. Model/reasoning/prompt defaults unchanged. |
| T-111 Add controlled Reorganise low-reasoning override | Completed | P2, backend cost/latency. Added explicit config for testing Reorganise with low reasoning before the later T-116 default switch. |
| T-112 Add opt-in Notes live Responses provider | Completed | P2, backend eval-to-runtime prep. Added the Notes live Responses strict-schema runtime candidate with safe fallback before the later T-115 default switch. |
| T-113 Harden Notes live Responses canary readiness | Completed | P2, backend reliability. Added safe provider/fallback diagnostics, fallback tests, and rollback documentation for the Notes live Responses provider before the later T-115 default switch. |
| T-114 Add process-heavy Summarise compression fixture | Completed | P1, backend quality. Added a dense RCA/process-style Summarise fixture and narrowly refined process-note compression guidance without changing model/reasoning defaults. |
| T-115 Make Notes live Responses default with Chat rollback | Completed | P1, backend live quality. Switched Notes live default provider to the Responses strict-schema path based on repeated evals, while preserving Chat fallback and env rollback. Forms live remains Chat. |
| T-116 Make Reorganise low reasoning default with medium rollback | Completed | P2, backend cost/latency. Changed Reorganise default reasoning to low with `FORMIFY_REORGANISE_REASONING=medium` rollback. Final notes/forms and Summarise remain medium. |
| T-117 Refine Summarise process/RCA compression guidance | Completed | P1, backend quality. Added targeted dense process/RCA compression guidance while preserving priority facts and keeping Summarise on medium reasoning. |

## Active

| ID | Status | Notes |
| --- | --- | --- |
| _None._ | | |

## Backlog

| ID | Status | Notes |
| --- | --- | --- |
| T-017 Long-session memory / rolling synthesis | Backlog | Add rolling checkpoint digests only if sessions beyond the current cap need bounded final-pass memory/cost. Backend-only; no WS protocol change. |
| T-070 Evaluate VAD-first audio filtering | Backlog | P2/P3, risky backend optimisation. Investigate moving VAD before Notes audio buffering so obvious silence is dropped before entering the transcription batch. Include risks around speech clipping, pre-roll/post-roll, CPU overhead, false skips, and stop-flush safety. Do not implement until batching/reconnect behaviour is stable. |
| T-073 Free-app usage safety limits and observability | Backlog | Add fair-use and content-safe usage observability only for reliability/cost-safety. No Stripe, Pro tiers, upgrades, or paywalls. |
| T-086 Evaluate prompt caching strategy | Backlog | P2/P3. Review prompt prefix stability, dynamic content placement, schema/instruction ordering, cached token logs, and safe `prompt_cache_key` usage. Use default/in-memory retention only unless a privacy decision explicitly allows extended retention. Do not treat caching as a substitute for reducing context bloat. |
| T-089 Trim redundant JSON shape validation after strict schema coverage | Backlog | P2/P3. After strict Responses schemas are proven stable, remove only redundant response-shape parsing, alias handling, and fallback parsing. Preserve semantic safety checks, stale guards, output sanity checks, privacy logging, final fallbacks, and Notes patch safety filters. |
| T-094 Reduce long-session live patch input context growth | Backlog | P2. Investigate reducing live Notes patch input tokens as canonical notes grow. Options include passing only relevant note sections, rolling compact context, limiting current notes window, or separating stable summary from mutable note tail. Preserve live quality and avoid hallucination. |
| T-095 Add long-session token/cost evals | Backlog | P2. Create fixtures or replay logs for long recordings to measure token growth, `currentNotesChars` growth, live patch input size, output size, cost per active minute, and latency. Use results before changing live context strategy. |
| T-097 Add Summarise compression evals | Backlog | P2. Add fixtures that measure input length vs summary length, heading count reduction, bullet count reduction, key fact preservation, hallucination avoidance, and open-question preservation. Use before further Summarise prompt/model changes. |
| T-098 Reorganise preservation evals | Backlog | P2/P3. Add fixtures to ensure Reorganise preserves 90-100% of useful detail, improves structure, preserves unresolved questions, and does not summarise too aggressively. |
| T-099 Explore app-owned async finalisation/transform jobs | Backlog | P2/P3. Design a privacy-first job/polling flow for long final Notes, Summarise, and Reorganise operations. Prefer app-owned jobs before provider background mode. Preserve preview/apply semantics and stale-result ignoring. |
| T-100 Assess Responses streaming for long final/transform UX | Backlog | P3. Investigate whether Responses streaming can improve perceived latency for final Notes or transforms without breaking strict JSON, preview-first Apply, stale-result ignoring, output validation, or safe logging. |
| T-101 Review provider background mode tradeoffs | Backlog | P3. Evaluate OpenAI background mode for long-running final/transform calls, including privacy/data retention implications, ZDR compatibility, polling/webhooks, timeout handling, and whether app-owned jobs are safer. Do not implement without explicit product/privacy approval. |
| T-102 Research stateful Responses for recording sessions | Backlog | P3. Assess `previous_response_id`, Conversations, manual output item replay, and encrypted reasoning items for recording continuity, live-to-final handoff, reconnects, and multi-stage Notes. Compare against app-owned canonical state and privacy-first requirements. |
| T-103 Prototype stateless multi-step Notes reasoning | Backlog | P3. Explore whether multi-step final Notes processing can improve quality while staying stateless and app-owned. Consider encrypted reasoning only if docs/privacy posture support it. Do not alter production finalisation without evals. |
| T-104 Map app-owned function tools for Formify | Backlog | P3. Design possible read-only and confirmation-gated function tools for template selection, custom block retrieval, checklist validation, follow-up task creation, tagging, export generation, and integration writes. Separate read-only tools from destructive/write tools. |
| T-105 Explore Ask about this recording | Backlog | P3. Research an opt-in Q&A feature over a completed recording/notes session using app-owned stored notes/transcript state. Evaluate privacy, retention, retrieval, cost, and whether Responses tools or file search are useful. |
| T-106 Explore document-grounded notes/forms | Backlog | P3. Research uploaded PDF/policy/checklist grounding for notes and forms. Assess file search, retrieval, user consent, source citations, privacy, storage, and product UX. |
| T-107 Explore template recommendation from session content | Backlog | P3. Evaluate whether the backend can recommend templates or custom blocks based on session content without risking latency or privacy. Should be opt-in or non-blocking. |
| T-108 Explore external integrations via tools/MCP | Backlog | P3. Research Notion, Google Docs, Calendar, CRM, or ticketing integrations through app-owned functions or MCP. Require explicit user consent, approval UX, and clear data-sharing boundaries. |
| T-109 Reject/avoid computer-use backend workflows | Backlog | P4. Document why computer-use style automation is not appropriate for the backend recording pipeline unless a future product requirement strongly justifies it. |

## Cross-Repo References

| Product ID | Status | Notes |
| --- | --- | --- |
| T-180 Notes transform pending/cancel/stale UX | Completed | Web repo reference. Notes Summarise/Reorganise use client-side run IDs, stale results are ignored, pending UX says notes are unchanged until Apply, and users can ignore long-running results. |
| T-181 Safe performance observability | Needs verification | Web/backend reference. Add or verify safe metadata logs for template queries/mutations, Notes transform bridge latency/errors, and backend observability. No raw content logging. |
| T-182 Add multi-step Notes undo/redo history | Backlog | Web repo reference. Replace one-step undo with bounded undo/redo for manual edits, Summarise/Reorganise Apply, final parse replacements, and template-related note changes where appropriate. |
